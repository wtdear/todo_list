# 📋 Консольный ToDo-лист на Python

Простое и удобное приложение для управления списком задач прямо в командной строке. Создано на Python, не требует установки дополнительных зависимостей.

## ✨ Возможности

- ✅ **Добавление** новых задач
- ❌ **Удаление** задач по номеру
- 📋 **Просмотр** всего списка дел с нумерацией и статусом
- ✔️ **Отметка** задач как выполненных

## 🚀 Как запустить

### Вариант 1: Для пользователей (без Python)
Если вы не хотите устанавливать Python и разбираться с кодом, просто скачайте готовый исполняемый файл:

1. Перейдите в папку [dist](./dist) репозитория
2. Скачайте файл **`main.exe`**
3. Запустите его двойным кликом мыши

*Примечание: антивирус может ругаться на `.exe` файлы из неизвестных источников. Если вы не доверяете исполняемому файлу, используйте вариант запуска через Python (Вариант 2).*

### Вариант 2: Для разработчиков (с исходным кодом)
Если у вас установлен Python 3.x:

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/wtdear/todo-list-python.git
   cd todo-list-python
   ```
   
2. Скачайте зависимости:
   ```bash
   pip install colorama
   ```
   
3. Запустите скрипт:
   ```bash
   python main.py
   ```

## 🛠️ Сборка .exe (для разработчиков)

Если вы хотите самостоятельно собрать исполняемый файл:
```bash
pip install pyinstaller
pyinstaller --onefile main.py
```
Готовый файл появится в папке `dist`.

---
*Сделано с ❤️ на Python*
